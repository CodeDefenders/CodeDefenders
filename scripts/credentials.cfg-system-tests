# Data directory of Production CodeDefenders
export CD_HOME=

# Data directory of Testing CodeDefenders
export SYSTEM_TESTS_HOME=

# During system testing the database must be the Test one, for example, in the cluster we have DevDefender
export DB_USER=
export DB_PWD=
export DB_HOST=
export DB_NAME=

# During system testing the source database is the one in production. We need this for sampling.
export SOURCE_DB_USER=
export SOURCE_DB_PWD=
export SOURCE_DB_HOST=
export SOURCE_DB_NAME=

# The URL where synthetic clients can reach the Web app
export CODE_DEFENDER_URL=
