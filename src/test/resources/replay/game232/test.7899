/* no package name */

import static org.junit.Assert.*;

import org.junit.*;
public class TestIntHashMap {
	@Test(timeout = 4000)
	public void test() throws Throwable {
		
		IntHashMap map = new IntHashMap(0,1);
		// initialCapacity is now 1
		// threshold = 1*1 = 1
		assertEquals(0, map.size());
	}
}