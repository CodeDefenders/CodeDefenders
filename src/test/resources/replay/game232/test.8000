/* no package name */

import static org.junit.Assert.*;

import org.junit.*;
public class TestIntHashMap {
	@Test(timeout = 4000)
	public void test() throws Throwable {
		try {
            new IntHashMap(-3);
            fail();
        } catch(IllegalArgumentException e){
            assertEquals(e.getMessage(), "Illegal Capacity: -3");
        }
	}
}