/* no package name */

import static org.junit.Assert.*;

import org.junit.*;
public class TestIntHashMap {
	@Test(timeout = 4000)
	public void test() throws Throwable {
		IntHashMap i = new IntHashMap(13, .99f);
        Integer o = 8;
        i.put(1,o);
        i.put(2,o);
        assertEquals(i.size(), 2);
        Integer t = (Integer) i.size();
	}
}