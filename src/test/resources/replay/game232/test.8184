/* no package name */

import static org.junit.Assert.*;

import org.junit.*;
public class TestIntHashMap {
	@Test(timeout = 4000)
	public void test() throws Throwable {
		// test here!
		boolean check = false;
		IntHashMap map = new IntHashMap(1,1);
		try {
			map.contains(null);
		} catch(NullPointerException e) {
			check = true;
		}
		
		assertTrue(check);
	}
}