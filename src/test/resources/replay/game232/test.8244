// @@ -145,6 +145,7 @@
//                  }
//              }
//          }
// +		tab[0] = null;
//          return false;
//      }
//  
//
/* no package name */

import static org.junit.Assert.*;

import org.junit.*;
public class TestIntHashMap {
	@Test(timeout = 4000)
	public void test() throws Throwable {
		// test here!
		IntHashMap map = new IntHashMap();
		map.put(3, new Integer(2));
		map.contains(new Integer(2));
		
		assertTrue(map.contains(new Integer(2)));
	}
}