// @@ -175,7 +175,7 @@
//      public boolean containsKey(int key) {
//          Entry tab[] = table;
//          int hash = key;
// -        int index = (hash & 0x7FFFFFFF) % tab.length;
// +        int index = (hash & 0x3FFFFFFF) % tab.length;
//          for (Entry e = tab[index]; e != null; e = e.next) {
//              if (e.hash == hash) {
//                  return true;
//
/* no package name */

import static org.junit.Assert.*;

import org.junit.*;
public class TestIntHashMap {
	@Test(timeout = 4000)
	public void test() throws Throwable {
		String b = "Hallo";
		IntHashMap map = new IntHashMap(3);
		map.put(Integer.MAX_VALUE - 2, b);
		assertEquals(map.containsKey(Integer.MAX_VALUE - 2), true);
	}
}